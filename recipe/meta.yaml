{% set name = "oceanum" %}
{% set version = "1.1.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/oceanum-{{ version }}.tar.gz
  sha256: 8613164818bc9e990e73c76730b860722e79c20acf2035052960262a04dea5b0

build:
  entry_points:
    - oceanum = oceanum.__main__:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.10
    - setuptools
    - pip
  run:
    - python >=3.10
    - aiohttp
    - click
    - cftime
    - dask
    - decorator
    - fsspec
    - geojson-pydantic
    - geopandas
    - h5netcdf
    - msgpack-python
    - numcodecs <0.16.0
    - numpy
    - orjson
    - pandas
    - platformdirs
    - pyarrow
    - pydantic
    - python-jsonpath
    - requests
    - shapely
    - tabulate
    - xarray
    - rioxarray
    - zarr <3
    - netcdf4


test:
  imports:
    - oceanum
  commands:
    - pip check
    - oceanum --help
  requires:
    - pip

about:
  home: https://github.com/oceanum-io/oceanum-python
  summary: Library for oceanum.io services
  dev_url: https://github.com/oceanum-io/oceanum-python
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - ondave
    - seboceanum
    - rafa-guedes
    - tomdurrant
